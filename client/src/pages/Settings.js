import React from "react";

const Settings = () => {
  return (
    <div className=" bg-primary w-screen h-screen text-text-primary text-center">
      Settings
    </div>
  );
};

export default Settings;
